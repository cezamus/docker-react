language: generic 
sudo: required
services:
    - docker
    
before_install:
    - docker build -t czarekpriv/docker-react -f Dockerfile.dev .
    
script:
    - docker run czarekpriv/docker-react npm run test -- --coverage